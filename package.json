{
  "name": "mock-cloud-controller",
  "version": "2.63.0",
  "main": "src/server_factory.js",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "node-uuid": "^1.4.7",
    "object.values": "^1.0.3"
  },
  "devDependencies": {
    "coveralls": "^2.11.14",
    "istanbul": "^0.4.5",
    "jasmine": "^2.5.2"
  },
  "scripts": {
    "generate-keys": "rm -rf sslcert && mkdir sslcert && openssl req -x509 -newkey rsa:2048 -keyout sslcert/key.pem -out sslcert/cert.pem -days 365 && openssl rsa -in sslcert/key.pem -out sslcert/newkey.pem && mv sslcert/newkey.pem sslcert/key.pem",
    "test": "istanbul cover --root ./src --include-all-sources node_modules/.bin/jasmine"
  }
}
